Set LOAD to None if you don't want to load an existing model. Otherwise it should be the file name of the model to load.
Set SAVE to True to save model to model_out.pt

Models for PyTorch ANN on MNIST digit and PyTorch CNN for both MNIST digit and fashion are provided.

No support for cifar yet.